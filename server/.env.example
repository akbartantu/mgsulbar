# Server
PORT=3001
NODE_ENV=development

# Frontend origin for CORS (comma-separated for multiple)
FRONTEND_URL=http://localhost:8080

# Google Sheets (add when implementing Sheets)
# GOOGLE_SHEETS_CREDENTIALS={"type":"service_account",...}
# SPREADSHEET_ID=your-spreadsheet-id

# Auth (add when implementing Google Sign-In)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Set BYPASS_AUTH=true to allow unauthenticated access to all API routes (e.g. when login is not used)
# BYPASS_AUTH=true

# Admin login (email + password, no registration)
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=your-secure-password
# JWT_SECRET=long-random-string-for-signing-admin-tokens

